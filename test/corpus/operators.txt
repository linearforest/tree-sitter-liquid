================================================================================
Equals
================================================================================

{% x == y %}

--------------------------------------------------------------------------------

(program
  (ERROR
    (UNEXPECTED '{'))
  (binary_expression
    (identifier)
    (comparision_operator)
    (identifier))
  (ERROR
    (UNEXPECTED '%')))

================================================================================
Not Equals
================================================================================

{% x != y %}

--------------------------------------------------------------------------------

(program
  (ERROR
    (UNEXPECTED '{'))
  (binary_expression
    (identifier)
    (comparision_operator)
    (identifier))
  (ERROR
    (UNEXPECTED '%')))

================================================================================
Greater than
================================================================================

{% x > y %}

--------------------------------------------------------------------------------

(program
  (ERROR
    (UNEXPECTED '{'))
  (binary_expression
    (identifier)
    (comparision_operator)
    (identifier))
  (ERROR
    (UNEXPECTED '%')))

================================================================================
Less than
================================================================================

{% x < y %}

--------------------------------------------------------------------------------

(program
  (ERROR
    (UNEXPECTED '{'))
  (binary_expression
    (identifier)
    (comparision_operator)
    (identifier))
  (ERROR
    (UNEXPECTED '%')))

================================================================================
Greater than or equal
================================================================================

{% x <= y %}

--------------------------------------------------------------------------------

(program
  (ERROR
    (UNEXPECTED '{'))
  (binary_expression
    (identifier)
    (comparision_operator)
    (identifier))
  (ERROR
    (UNEXPECTED '%')))

================================================================================
Less than or equal
================================================================================

{% x >= y %}

--------------------------------------------------------------------------------

(program
  (ERROR
    (UNEXPECTED '{'))
  (binary_expression
    (identifier)
    (comparision_operator)
    (identifier))
  (ERROR
    (UNEXPECTED '%')))

================================================================================
Contains
================================================================================

{% x contains y %}

--------------------------------------------------------------------------------

(program
  (ERROR
    (UNEXPECTED '{'))
  (binary_expression
    (identifier)
    (comparision_operator)
    (identifier))
  (ERROR
    (UNEXPECTED '%')))

================================================================================
Or
================================================================================

{% x or y %}

--------------------------------------------------------------------------------

(program
  (ERROR
    (UNEXPECTED '{'))
  (binary_expression
    (identifier)
    (logical_operator)
    (identifier))
  (ERROR
    (UNEXPECTED '%')))

================================================================================
And
================================================================================

{% x and y %}

--------------------------------------------------------------------------------

(program
  (ERROR
    (UNEXPECTED '{'))
  (binary_expression
    (identifier)
    (logical_operator)
    (identifier))
  (ERROR
    (UNEXPECTED '%')))

================================================================================
Contains
================================================================================

{% x contains y %}

--------------------------------------------------------------------------------

(program
  (ERROR
    (UNEXPECTED '{'))
  (binary_expression
    (identifier)
    (comparision_operator)
    (identifier))
  (ERROR
    (UNEXPECTED '%')))

================================================================================
Operator precedence
================================================================================

{% true and false and false or true %}

--------------------------------------------------------------------------------

(program
  (ERROR
    (UNEXPECTED '{'))
  (binary_expression
    (boolean)
    (logical_operator)
    (binary_expression
      (boolean)
      (logical_operator)
      (binary_expression
        (boolean)
        (logical_operator)
        (boolean))))
  (ERROR
    (UNEXPECTED '%')))
